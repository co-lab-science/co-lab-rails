<%= form_for(hypothesis) do |f| %>
  <% if hypothesis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hypothesis.errors.count, "error") %> prohibited this hypothesis from being saved:</h2>

      <ul>
      <% hypothesis.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <h2>New Hypothesis</h2>

<!--   <div class="field">
    <%= f.label :author %>
    <p>creator's username </br>created at xx/xx/xx</p>
  </div> -->
  
  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title, class:'summernote'%>
  </div>

  <div class="field" >
    <%= f.label :abstract %>
    <%= f.text_field :body, class:'summernote', value:"Hello Moto" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<script>
$('.summernote').summernote({
    airMode: true,
    height: 1000,
    minHeight: null,
    maxHeight: null,
    focus: true
});
</script>
