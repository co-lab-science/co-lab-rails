<main>
<section class="general-info" id="content-meta-info" data-content-id="<%= @hypothesis.id %>" data-userid="<%= current_user.try(:id) %>" data-content-type="<%= @hypothesis.class%>">
  <div class="general-overview">
    <h2 class="general-info-title"><%= link_to 'All Labs', observations_path %></h2>
  </div>
  <div class="general-comments">
    <div class="observation-tags">
      <div class="comment">
        <div class="comment-type type-observation"> + obesrvation</div>
        <div class="comment-type type-question"> + question</div>
        <div class="comment-type type-hypothesis"> + hypothesis</div>
        <div class="comment-type type-citation"> + citation</div>
      </div>
      <div id="observation-container-jq" class="comments-container-jq">
      </div>
      <div id="questions-container-jq" class="comments-container-jq">
      </div>
      <div id="hypothesis-container-jq" class="comments-container-jq">
      </div>
      <div id="citations-container-jq" class="comments-container-jq">
        <% if current_user.try(:id) == @hypothesis.user_id %>
        <input id="fileupload" type="file" name="files[]" data-url="/upload-file?hypothesis_id=<%= @hypothesis.id %>" multiple>
      <% end  %>
        <ul>
        <% @hypothesis.lab_files.each do |file| %>
          <li> <%= link_to file.name, file.url%> </li>
        <% end %>
        </ul>
      </div>
    </div>
</section>
<section class="hypothesis-editor detail-text-view">
  <div class="general-hypothesis-modifier">
    <div>
      <h2 class="general-lab-upvote">Upvote<span><%= @hypothesis.upvotes.count %></span></h2>
      <h2 class="general-lab-downvote">Downvote<span><%= @hypothesis.downvotes.count %></span></h2>
    </div>
  </div>
  <div class="lab-tags">
    <% @hypothesis.tags.each_with_index do |tag, index| %>
      <div class="lab-tag applied"><%= tag.name %></div>
    <% end %>
  </div>
  <h2><%= @hypothesis.title %></h2>
  <span class="comment">
    <span class="user">
      <span class="lighten">submitted by </span>
      <%= User.find(@hypothesis.user_id).name %><br>
      <span class="lighten"><%= @hypothesis.created_at %></span>
      <% if (current_user && current_user.id == @hypothesis.user_id) %>
        <br>
        <span class="lighten"> <%= link_to "Edit", edit_observation_path(@hypothesis.id) %> </span>
      <% end %>
    </span>
  </span><br>
  <p id="notice"><%= notice %></p>
  <br>
  <p>
  <%= sanitize @hypothesis.body %>
  </p>
</section>
<section class="hypothesis-editor visualization-view hidden">
  <div class="hypothesis-graphic"></div>
</section>
</main>


