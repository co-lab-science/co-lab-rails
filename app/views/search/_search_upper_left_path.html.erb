<% if params[:parent] %>
  <% if params[:parent_type] =="Lab" %>
    <%= link_to "Back to Previous Post", "/observations/#{params[:parent]}" %>
  <% elsif params[:parent_type] =="Question" %>
    <%= link_to "Back to Previous Post", "/questions/#{params[:parent]}" %>
  <% elsif params[:parent_type] =="Hypothesis" %>
    <%= link_to "Back to Previous Post", "/hypothesis/#{params[:parent]}" %>
  <% end %>
<% else %>
  <%= link_to 'Try a new search', '/search?utf8=✓&search_term=&commit=Search' %>
<% end %>
